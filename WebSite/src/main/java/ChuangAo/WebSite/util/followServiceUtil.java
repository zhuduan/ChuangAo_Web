package ChuangAo.WebSite.util;

public class followServiceUtil {
	
	
	 public static Integer updateOfflineStatus(Integer states,Integer accountID){
		 
		 //---TODO: 
		 //1.check the former status, 0 for not monitor
		 //2.update the info in the db (if not 0)
		 //3.tell the front page to show the new result
		 
		 return 0;
	 }
	 
	 
	 public static Integer updateFeedBackStatus(Integer states,Integer accountID,String fbInfo){
		 
		 //---TODO: 
		 //1.check the former status, 0 for not effect
		 //2.update the info in the db (if not 0)
		 //3.tell the front page to show the new result (get the fb string in the db)
		 
		 return 0;
	 }
	 
	 
	 public static Integer updateDataStatisticStatus(Integer states,Integer accountID,String datas){
		 
		 //---TODO: 
		 //1.check the former status, 0 for not effect
		 //2.update the info in the db (if not 0)
		 //3.tell the front page to show the new result (get the datas string in the db)
		 
		 return 0;
	 }
	
}
